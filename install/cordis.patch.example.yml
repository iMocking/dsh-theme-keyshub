# dsh-theme-triptych — loader insert example.
# Append to $DSH_HOME/profiles/<profile>/cordis.patch.yml (or replace the `[]`
# placeholder file entirely), then restart dsh.
- insert:
    - id: dsh-theme-triptych
      name: dsh-theme-triptych
